{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "9router-quota-tracker",
  "title": "9Router Quota Tracker",
  "description": "Track 9Router LLM provider account quotas and usages",
  "icon": "assets/extension-icon.png",
  "author": "fadlee",
  "categories": [
    "Developer Tools",
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "index",
      "title": "Track Quotas",
      "description": "View 9Router provider account quota details and usages",
      "mode": "view"
    },
    {
      "name": "background",
      "title": "Refresh Quota Cache",
      "description": "Refresh cached 9Router quota data",
      "mode": "no-view",
      "interval": "5m"
    }
  ],
  "preferences": [
    {
      "name": "baseUrl",
      "type": "textfield",
      "required": false,
      "title": "9Router Base URL",
      "description": "Base URL of your 9Router instance (e.g. http://localhost:3000)",
      "placeholder": "http://localhost:3000"
    },
    {
      "name": "password",
      "type": "password",
      "required": false,
      "title": "9Router Password",
      "description": "Password for 9Router login"
    },
    {
      "name": "providerFilter",
      "type": "textfield",
      "required": false,
      "title": "Provider Filter",
      "description": "Filter by provider name (e.g. antropic, openai, or 'all')",
      "default": "all"
    }
  ],
  "dependencies": {
    "@raycast/api": "^1.79.0",
    "@raycast/utils": "^1.16.0",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.11",
    "@types/node": "^26.2.0",
    "@types/react": "^19.2.18",
    "typescript": "^7.0.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop"
  }
}
